---
interface Props {
	src: string;
	albumId: string;
	alt?: string;
}

const { src, albumId, alt = "" } = Astro.props;

// 缩略图路径：从原图路径推导 _thumb.webp
const thumbSrc = src.replace(/\.(jpe?g|png|avif|gif)$/i, "_thumb.webp");
---

<div class="gallery-photo-card break-inside-avoid mb-3">
  <div
    data-fancybox={`gallery-${albumId}`}
    data-src={src}
    data-type="image"
    class="block rounded-xl overflow-hidden group cursor-pointer"
  >
    <img
      src={thumbSrc}
      data-fullsrc={src}
      alt={alt}
      loading="lazy"
      decoding="async"
      class="w-full h-auto object-cover transition-transform duration-300 group-hover:scale-105"
    />
  </div>
</div>
